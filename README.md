# Screen-Pause (or Shape-Simulator)
Pause screen parts or simulate shapes for research prototypes.
## Installation
pip install -e .
## Usage
from screen_pause.core import ShapeSimulator
sim = ShapeSimulator()
sim.generate_and_save('circle', 'test')
## Structure
….

